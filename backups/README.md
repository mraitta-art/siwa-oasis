# Backup Notes

This folder contains local emergency backups for uploaded media.

Use:
- npm run backup:media

The backup script copies the local media upload folder into a timestamped backup directory under this folder.

For Git access, keep the backup folder under version control or copy the archive to a GitHub-connected storage location if needed.
